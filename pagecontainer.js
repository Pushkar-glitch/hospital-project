$(document).ready(() => {
    window.onscroll = function() { myFunction() };

    function myFunction() {
        //   if (document.documentElement.scrollTop > 50) {
        //   document.getElementsByClassName("doc-head").className = "test";
        // } else {
        //   document.getElementsByClassName("doc-head").className = "";
        //};
    };
    $(".doc-head").scrollDown > 50(() => {

        $(".doc-head").hide();

    })
});
$(document).ready(() onabort onabort)[

]